apiVersion: apps/v1
kind: Deployment
metadata:
  name: symfony-app
  labels:
    app: symfony-devops
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ici
  template:
    metadata:
      labels:
        app: ici
    spec:
      containers:
        - name: ici
          image: ici
          ports:
            - containerPort: 8080